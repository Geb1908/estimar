class Computo {
  //constructor
  constructor(
    cartelas,
    bulones,
    parantes,
    pasamanos,
    guardarrodilla,
    guardapie,
    extremos,
    perfil
  ) {
    (this.cartelas = cartelas),
      (this.bulones = bulones),
      (this.parantes = parantes),
      (this.pasamanos = pasamanos),
      (this.guardarrodilla = guardarrodilla),
      (this.guardapie = guardapie),
      (this.extremos = extremos),
      (this.perfil = perfil);
  }
}
