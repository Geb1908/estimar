class Baranda {
  //constructor
  constructor(material, calidad, tipo) {
    this.material = material.toUpperCase();
    this.calidad = calidad.toUpperCase();
    this.tipo = tipo.toUpperCase();
  }
}
