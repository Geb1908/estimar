class Obra {
  //constructor
  constructor(empresa, cliente, proyecto, lugar) {
    this.empresa = empresa.toUpperCase();
    this.cliente = cliente.toUpperCase();
    this.proyecto = proyecto.toUpperCase();
    this.lugar = lugar.toUpperCase();
  }
}
